---
title: Serializer Workflow
description: Export and import JSON Canvas without leaving the page.
navigation:
  icon: i-lucide-file-json
---

Vue Board keeps the serializer branded around the external JSON Canvas format because that format is interoperable outside this repo.

::serializer-board-demo
::
