---
title: Renderers and Layout
description: Swap node bodies without replacing board interaction.
navigation:
  icon: i-lucide-layout-template
---

Custom renderers are just Vue components keyed by node type.

```ts
const renderers = {
  insight: DocsInsightNode
}
```

Use them when the default text-card rendering is too generic, but keep the board shell in charge of selection, dragging, resizing, and edit state.

::renderer-board-demo
::
