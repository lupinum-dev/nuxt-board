# @lupinum/vue-board

Vue components and composables for `@lupinum/board-core`. Provides `<BoardRoot>`, `<BoardNode>`, and the composables that translate pointer / keyboard input into engine commands. Composables are public — use them to assemble a custom root component when the default `<BoardRoot>` doesn't fit.
