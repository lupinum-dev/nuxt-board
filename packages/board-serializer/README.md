# @lupinum/board-serializer

Save and restore boards as [JSON Canvas](https://jsoncanvas.org/) documents. Handles the core node/grid/camera state plus edges from `@lupinum/board-connections` when that plugin is installed. Custom node `type` handlers extend the serializer.
