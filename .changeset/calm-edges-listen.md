---
'@lupinum/board-connections': major
'@lupinum/board-core': patch
---

Expose immutable, identity-stable edge snapshots through `$edges`. Persisted edge data now accepts JSON objects only and is recursively frozen.
