# Vue Board Docs

This Nuxt app powers the Vue Board documentation site. It imports the workspace packages directly, embeds live board demos, and serves maintained API reference pages from `apps/docs/content/6.api`.

## Local development

```bash
pnpm --filter docs dev
```

## Build

```bash
pnpm --filter docs build
```
